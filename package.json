{
  "name": "ai-pm-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Test configuration for the static project management dashboard",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
